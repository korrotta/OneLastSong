<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OneLastSong.Views.EqualizerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OneLastSong.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel>
        <TextBlock Text="Equalizer" FontSize="20" Margin="10"/>
        <ItemsControl ItemsSource="{x:Bind ViewModel.EqualizerViewModel.Bands}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <TextBlock Text="{Binding Frequency}" Width="50"/>
                        <Slider Minimum="-10" Maximum="10" Value="{Binding Gain, Mode=TwoWay}" Width="200"/>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <Button Content="Apply" Click="ApplyEQ_Click" Margin="10"/>
    </StackPanel>
</Page>
