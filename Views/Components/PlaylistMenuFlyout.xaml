<?xml version="1.0" encoding="utf-8"?>
<MenuFlyout
    x:Class="OneLastSong.Views.Components.PlaylistMenuFlyout"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OneLastSong.Views.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <MenuFlyout.MenuFlyoutPresenterStyle>
        <Style TargetType="MenuFlyoutPresenter">
            <Setter Property="Background" Value="{ThemeResource BG_LIGHT_TERTIARY}" />
        </Style>
    </MenuFlyout.MenuFlyoutPresenterStyle>

    <MenuFlyoutItem Text="Play" Foreground="{ThemeResource TEXT_PRIMARY}" Click="PlayPlaylist_Click"/>
    <MenuFlyoutItem Text="Add to Queue" Foreground="{ThemeResource TEXT_PRIMARY}" Click="AddToQueue_Click"/>
    <MenuFlyoutItem Text="Songs" Foreground="{ThemeResource TEXT_PRIMARY}" Click="SongsButton_Click"/>
    <MenuFlyoutItem Text="Remove" Foreground="{ThemeResource TEXT_PRIMARY}" Click="RemovePlaylist_Click"/>
    <MenuFlyoutSeparator Foreground="{ThemeResource TEXT_PRIMARY}"/>
    <MenuFlyoutItem Text="Details" Foreground="{ThemeResource TEXT_PRIMARY}" Click="EditPlaylistDetails_Click"/>

</MenuFlyout>