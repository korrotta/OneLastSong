<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OneLastSong.Views.Components.BodyFrame"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OneLastSong.Views.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent"
    >
    
    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <!-- Define your body content here -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="31*"/>
            <ColumnDefinition Width="38*"/>
            <ColumnDefinition Width="31*"/>
        </Grid.ColumnDefinitions>

        <Frame x:Name="LefRegion" Grid.Column="0" Margin="15 0 0 0" Background="{ThemeResource BG_TERTIARY}" CornerRadius="13"/>
        <Frame x:Name="CenterRegion" Grid.Column="1" Margin="15 0 15 0" Background="{ThemeResource BG_TERTIARY}" CornerRadius="13"/>
        <Frame x:Name="RightRegion" Grid.Column="2"  Margin="0 0 15 0" Background="{ThemeResource BG_TERTIARY}" CornerRadius="13"/>
    </Grid>
</Page>
