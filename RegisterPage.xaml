<Page
    x:Class="OneLastSong.RegisterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="#f0f0f0">
    <Grid>
        <Border Background="White" CornerRadius="10" Padding="20" Margin="20" VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
            <StackPanel>
                <TextBlock Text="Register" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>
                <TextBox x:Name="UsernameRegister" PlaceholderText="Username" Margin="0,0,0,10" Padding="10" BorderBrush="#ccc" BorderThickness="1" CornerRadius="5"/>
                <PasswordBox x:Name="PasswordRegister" PlaceholderText="Password" Margin="0,0,0,10" Padding="10" BorderBrush="#ccc" BorderThickness="1" CornerRadius="5"/>
                <Button Content="Register" Click="Register_Click" Margin="0,0,0,10" Background="#0078D7" Foreground="White" Padding="10" BorderBrush="#0078D7" BorderThickness="1" CornerRadius="5"/>
                <Button Content="Back to Login" Click="NavigateToLoginPage_Click" Background="Transparent" Foreground="#0078D7" Padding="10" BorderBrush="Transparent" BorderThickness="1" CornerRadius="5"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>


